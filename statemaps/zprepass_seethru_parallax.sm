alphaTest
{
	default:
		GT0;
}

blendFunc
{
	default:
		Disable, One, Zero;
}

separateAlphaBlendFunc
{
	default:
		Disable, One, Zero;
}

cullFace
{
	default:
		passthrough;
}

depthTest
{
	default:
		passthrough;
}

depthWrite
{
	default:
		Disable;
}

colorWrite
{
	default:
		Disable, Disable;
}

polygonOffset
{
	default:
		passthrough;
}

stencil
{
	default:
		OneSided,
		Always, IncrSat, Keep, Keep,
		Always, IncrSat, Keep, Keep;
}

wireframe
{
	default:
		Disable;
}
