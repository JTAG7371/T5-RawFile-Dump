con_showchannel *

exec default_mp_gamesettings.cfg

// PC - default server rotation for maps only:

exec server_map.cfg

// ==== all of below should be moved to code ====

// PC - default server settings:
set scr_game_allowkillcam	1
